# Issue Tracker: GitHub

This project uses GitHub Issues for task tracking and PRD storage.

## Workflow

- **Creation**: Use `gh issue create --title "<title>" --body "<body>" --label "<label>"`.
- **Reading**: Use `gh issue list` and `gh issue view <number>`.
- **Linking**: PRDs and issues should reference each other via issue numbers.
